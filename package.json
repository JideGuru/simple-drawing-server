{
    "name": "simple-drawing-server",
    "version": "0.0.1",
    "description": "my first socket.io app",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "nodemon index.js",
        "dev": "nodemon index.js"
    },
    "dependencies": {
        "express": "^4.18.2",
        "socket.io": "^4.5.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.20"
    }
}